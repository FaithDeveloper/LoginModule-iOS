# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LoginModel' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LoginModel

  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  pod 'Firebase/Database'

  target 'LoginModelTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LoginModelUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
